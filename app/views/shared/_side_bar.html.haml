.chat__side__top
  .chat__side__top__user-name
    = current_user.name
  .chat__side__top__icon
    = link_to new_group_path do
      = fa_icon 'pencil-square-o', class: 'icon'
    = link_to edit_user_path(current_user) do
      = fa_icon 'cog', class: 'icon'
  .chat__side__glist
  - current_user.groups.each do |group|  
    .chat__side__glist__group    
      = link_to group_messages_path(group) do
        .chat__side__glist__group__name
          = group.name
        .chat__side__glist__group__message
          = group.show_last_message
  -# .groups
  -#   - current_user.groups.each do |group|
  -#     .group
  -#       = link_to group_messages_path(group) do
  -#         .group__name
  -#           = group.name
  -#         .group__message
  -#           メッセージはまだありません。

-# .contents__left
-#   .contents__left__top
-#     .contents__left__top__box
-#       .contents__left__top__box__text
-#         = current_user.name
-#       .contents__left__top__box__image 
-#         .contents__left__top__box__image__cog
-#           .fa.fa-cog
-#         .contents__left__top__box__image__edit
-#           .fa.fa-edit

-#   .contents__left--bottom
-#     .contents__left--bottom__groups
-#       - current_user.groups.each do |group|
-#         .contents__left--bottom__groups__group
-#           = link_to group_messages_path(group) do
-#             .contents__left--bottom__groups__group__name
-#               = group.name
-#           .contents__left--bottom__groups__group__message
-#             = group.show_last_message
