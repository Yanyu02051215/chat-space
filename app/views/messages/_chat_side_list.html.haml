.chat__side__list
  - current_user.groups.each do |group|  
    .chat__side__list__group    
      = link_to group_messages_path(group) do
        .chat__side__list__group__name
          = group.name
        .chat__side__list__group__message
          = group.show_last_message